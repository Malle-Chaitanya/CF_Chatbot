# Phase 1 backend dependencies
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Weaviate
weaviate-client>=4.0.0

# OpenAI (embeddings + optional LLM)
openai>=1.0.0

# Token-based chunking (tiktoken for OpenAI tokenizer)
tiktoken>=0.5.0

# HTTP
httpx>=0.25.0

# HTML parsing for heading-aware blog chunking
beautifulsoup4>=4.12.0

# API server (Phase 2 search)
fastapi>=0.100.0
uvicorn[standard]>=0.24.0


